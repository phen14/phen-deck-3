// (K) ALL RIGHTS REVERSED - Reprint what you like

/**
 * Class representation of the account options in the configuration file.
 */
export type AccountOptions = {
    // Should this account be selected by default to post with?
    primary: boolean;
}
