// (K) ALL RIGHTS REVERSED - Reprint what you like

/**
 * Class representation of the server data in the configuration file.
 */
export class Server {
    url: string;

    constructor(url: string) {
        this.url = url;
    }
}
